name: Ultimate-Gaming-VM-V41-MAX-FPS
on: workflow_dispatch
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Keep Alive
        run: |
          echo "VM is running. Connect via AnyDesk."
          sleep 21600
